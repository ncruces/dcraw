name: Distribution

on:
  workflow_dispatch:
  
jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Build artifact
      run: ./build.sh

    - name: Upload artifact
      uses: actions/upload-artifact@v3
      with:
        path: dcraw.wasm.gz